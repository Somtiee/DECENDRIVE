/** On-chain File.metadata keys (upsert_metadata_key). */

export const META_DISPLAY_NAME = "display_name";

export const META_MIME_TYPE = "mime_type";

/** Share permission flags stored on File metadata (view / download). */
export const META_LINK_VIEW = "link_view";

export const META_LINK_DOWNLOAD = "link_download";

/** Comma-separated recipient addresses whose share access was revoked by the owner (legacy). */

export const META_SHARE_REVOKED = "share_revoked_recipients";

/** Comma-separated ShareInvitation object IDs whose access was revoked by the owner. */

export const META_SHARE_REVOKED_INVITATIONS = "share_revoked_invitations";

